// ────────────────────────────────────────────────────────────
// Platform  : LeetCode
// Problem   : Valid Parentheses
// Difficulty: Easy
// Language  : java
// URL       : https://leetcode.com/problems/valid-parentheses/
// Solved    : 2026-05-28T17:07:41.138Z
// ────────────────────────────────────────────────────────────

                }
                    return false;
                if (ch == ')' && top != '(') {
                char top = stack.pop();
                }
                    return false;
                stack.push(ch);
            if (ch == '(' || ch == '[' || ch == '{') {
        for (char ch : s.toCharArray()) {
        Stack<Character> stack = new Stack<>();
    public boolean isValid(String s) {
class Solution {
            } else {
                if (stack.isEmpty()) {
                if (ch == ']' && top != '[') {
                    return false;
